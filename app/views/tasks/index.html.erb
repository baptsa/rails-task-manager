<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Task index</title>
</head>
<body>

  <div class="head"><h1>Todo list</h1>
    <h3><%= link_to "Add a task", new_task_path %></h3>
    <h2>Voici vos tâches</h2>
  </div>

  <% @tasks.each do |task| %>
  <div class="task">
    <h2><%= link_to task.name , task_path(task) %></h2>
    <h3><%= task.done %></h3>
    <h2><%= link_to task.name , task_path(task) %></h2>

  </div>


<% end %>




</body>
</html>
